# React Frontend

Start when you've practiced with the vanilla frontend.

```bash
npm install
npm run dev
```

Runs at http://localhost:5173 — API calls go through `/api/*` proxy to the Python backend.
