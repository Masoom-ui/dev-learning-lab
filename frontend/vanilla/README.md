# Vanilla Frontend (HTML + JS)

Open `index.html` in a browser, or use the **Live Server** extension in Cursor.

**Start the backend first:**

```bash
cd backend
uvicorn app.main:app --reload
```

Then open the page and click **Check backend** to verify the connection.
